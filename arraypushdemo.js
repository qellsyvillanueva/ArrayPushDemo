//build an array that stores first names. The default value of the array will be empty
var joseph = [
  ];
var landon = [
 ];
// ask user for first name, and store it into the first names array.
	var first = prompt("What is your first name?");
  jospeh = first;
//Ask usrer for a last name, and store it into the last names array
	var last = prompt("What is your last name?");
  landon = last;
//display to the window both first and last name from the array with the length of the both arrays

window.alert(first[0] + last[0] + first.length + last.length);

